---
import { APP_BLOG } from 'astrowind:config';
import type { Post } from '~/types';

import Image from '~/application/shared/components/ui/Image.astro';

import { findImage } from '~/application/shared/utils/images';
import { getPermalink } from '~/application/shared/utils/permalinks';

export interface Props {
  post: Post;
}

const { post } = Astro.props;
const image = await findImage(post.image);

const link = APP_BLOG?.post?.isEnabled ? getPermalink(post.permalink, 'post') : '';
---

<article class="mb-6 transition">
  <div class="relative mb-6 rounded bg-gray-400 shadow-lg dark:bg-slate-700 md:h-64">
    {
      image &&
        (link ? (
          <a href={link}>
            <Image
              src={image}
              class="w-full rounded bg-gray-400 shadow-lg dark:bg-slate-700 md:h-full"
              widths={[400, 900]}
              width={400}
              sizes="(max-width: 900px) 400px, 900px"
              alt={post.title}
              aspectRatio="16:9"
              layout="cover"
              loading="lazy"
              decoding="async"
            />
          </a>
        ) : (
          <Image
            src={image}
            class="w-full rounded bg-gray-400 shadow-lg dark:bg-slate-700 md:h-full"
            widths={[400, 900]}
            width={400}
            sizes="(max-width: 900px) 400px, 900px"
            alt={post.title}
            aspectRatio="16:9"
            layout="cover"
            loading="lazy"
            decoding="async"
          />
        ))
    }
  </div>

  <h3 class="font-heading mb-2 text-xl font-bold leading-tight dark:text-slate-300 sm:text-2xl">
    {
      link ? (
        <a class="hover:text-primary inline-block transition duration-200 ease-in dark:hover:text-blue-700" href={link}>
          {post.title}
        </a>
      ) : (
        post.title
      )
    }
  </h3>

  <p class="text-muted text-lg dark:text-slate-400">{post.excerpt}</p>
</article>
