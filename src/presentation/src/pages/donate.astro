---
import RedirectLayout from "~/presentation/src/shared/layouts/RedirectLayout.astro";
import LinksData, { Links } from "~/domain/data/Links";

const targetUrl = LinksData.find((l) => l.id === Links.donate)!.url;
---
<RedirectLayout
  title="Redirecting to Donation Page"
  description="Redirecting to Ahmet Çetinkaya's donation page"
  targetUrl={targetUrl}
  serviceName="donation page"
/>
