---
import RedirectLayout from "~/presentation/src/shared/layouts/RedirectLayout.astro";
import LinksData, { Links } from "~/domain/data/Links";

const targetUrl = LinksData.find((l) => l.id === Links.email)!.url;
---
<RedirectLayout
  title="Redirecting to Email"
  description="Redirecting to Ahmet Çetinkaya's email address"
  targetUrl={targetUrl}
  serviceName="email"
/>
