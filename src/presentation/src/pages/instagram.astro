---
import RedirectLayout from "~/presentation/src/shared/layouts/RedirectLayout.astro";
import LinksData, { Links } from "~/domain/data/Links";

const targetUrl = LinksData.find((l) => l.id === Links.instagram)!.url;
---

<RedirectLayout
  title="Redirecting to Instagram"
  description="Redirecting to Ahmet Çetinkaya's Instagram profile"
  targetUrl={targetUrl}
  serviceName="Instagram"
/>
