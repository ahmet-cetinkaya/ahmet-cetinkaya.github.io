---
import RedirectLayout from "../shared/layouts/RedirectLayout.astro";
import LinksData, { Links } from "@domain/data/Links";

const targetUrl = LinksData.find((l) => l.id === Links.linkedin)!.url;
---

<RedirectLayout
  title="Redirecting to LinkedIn"
  description="Redirecting to Ahmet Çetinkaya's LinkedIn profile"
  targetUrl={targetUrl}
  serviceName="LinkedIn"
/>
