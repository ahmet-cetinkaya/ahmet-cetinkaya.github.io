export default class HomeDirectories {
  static readonly HOME = "/home/ac";
  static readonly MODELS = `${HomeDirectories.HOME}/Models`;
  static readonly GAMES = `${HomeDirectories.HOME}/Games`;
  static readonly LIBRARIES = `${HomeDirectories.HOME}/Libraries`;
  static readonly PICTURES = `${HomeDirectories.HOME}/Pictures`;
  static readonly DESKTOP = `${HomeDirectories.HOME}/Desktop`;
}
