import HomeDirectories from "./HomeDirectories";

export default class ModelPaths {
  static readonly COMPUTER = `${HomeDirectories.MODELS}/computer/retro-computer.glb`;
  static readonly ENVELOPE = `${HomeDirectories.MODELS}/envelope/envelope.glb`;
  static readonly TERMINAL = `${HomeDirectories.MODELS}/terminal/terminal.glb`;
  static readonly DOOM = `${HomeDirectories.MODELS}/doom/doom.glb`;
}
